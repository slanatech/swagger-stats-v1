/*
 * UBook: Prometheus transforms. Convert prometheus data to various other formats.
 */
const { pathOr } = require('ramda');

class SpanTransforms {
  constructor() {
    // TODO
  }

  flatten(prometheusData, start, end, step) {
    // +++
  }
}

let spanTransforms = new SpanTransforms();
export default spanTransforms;
